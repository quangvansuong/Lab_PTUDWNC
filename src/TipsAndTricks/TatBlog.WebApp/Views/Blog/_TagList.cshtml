@model IList<Tag>

@*// Hiển thị từ khóa bài viết*@
@foreach (var item in Model)
{
    <a asp-controller="BLog"
    asp-action="Tag"
    asp-route-slug="@item.UrlSlug"
    title="Tag: @item.Name"
    class="btn btn-sm btn-outline-secondary">
        @item.Name
    </a>
}

